import axios from 'axios'; export async function goToCheckout(cartItems){ const res = await axios.post('/api/create-checkout-session', { items: cartItems }); if(res.data?.url) { window.location.href = res.data.url; return; } throw new Error('Checkout failed'); }
